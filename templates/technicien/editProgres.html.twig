{% extends 'base.html.twig' %}

{% block title %}Hello ResponsableController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="col-md-8 col-md-offset-5">
        <div class="alert alert-dismissible alert-success">
  
  <h4 class="alert-heading"> <u>
  Quel est le progrès de votre ordre de travail ?
  
  </u> </h4>
</div>
            {{form_start(formTache)}}
    <div class="form-group">
        {{form_row(formTache.progress, {'label' : 'Progrès :','attr' : 
    {'placeholder' : 'Progrès de la tache...' , 'class': "custom-range"}})}}
    </div>
    
    
    <div class="form-group">
        <button type="submit" class="btn btn-success">
        
                Valider
        </button>
    </div>

    {{form_end(formTache)}}
    
    
    {#{{dump(formTache)}}#}
        </div>
    </div>
{% endblock %}