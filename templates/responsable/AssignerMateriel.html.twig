{% extends 'base.html.twig' %}

{% block title %}Hello ResponsableController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="col-md-6 col-md-offset-5">
        <div class="alert alert-dismissible alert-success">
  
  <h4 class="alert-heading"> <u>
  Attribuer ce matériel à un ordre de travail
  
  </u> </h4>
</div>
            {{form_start(formMateriel)}}
    

    <div class="form-group">
         {{form_row(formMateriel.tache, {'label':"Ordre de travail...", 'attr':{ 'class': "form-control" }})}}
    </div>
    
   
    
    <div class="form-group">
        <button type="submit" class="btn btn-success">
            Valider
        </button>
    </div>

    {{form_end(formMateriel)}}
    {{dump(formMateriel)}}
    
        </div>
    </div>
{% endblock %}